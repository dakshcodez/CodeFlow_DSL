// Expected diagnostic (semantic analysis): undefined identifier 'temperature'.
// Syntactically valid; the semantic analyzer's scope checking reports this.
workflow "Test" {
    when temperature > 30 {
        log "Hot"
    }
}
