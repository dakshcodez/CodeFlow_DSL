// Expected diagnostic (semantic analysis): 'boolean + number' is not a valid operation.
// Syntactically valid; the semantic analyzer's type checking reports this.
workflow "Test" {
    input active : boolean

    when active + 5 {
        log "Invalid"
    }
}
